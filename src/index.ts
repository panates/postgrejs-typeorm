/**
 * Placeholder. The facade is not written yet - see `doc/DRIVER-DESIGN.md` for
 * what it has to be, and its "Decisions that need you" section for what is
 * still open. The proposed file layout is in that document's "Proposed layout".
 *
 * This file exists so the toolchain (`lint`, `compile`, `check`) has an entry
 * point to resolve; replace it, do not add to it.
 */
export {};
